
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>Admin Team Management</title>
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <meta content="" name="keywords">
    <meta content="" name="description">

        <!-- Favicon -->
        <link rel="icon" href="public\images\DIB-NOW-512.png" type="image/x-icon">
    
    <!-- Icon Font Stylesheet -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Customized Bootstrap Stylesheet -->
    <link href="\bootstrap.css" rel="stylesheet">
    <!-- Template Stylesheet -->
    <link href="\styles.css" rel="stylesheet">
    <link href="\styles1.css" rel="stylesheet">
    <style>
      .body-wrapper {
      min-height: 100%;
      display: flex;
      flex-direction: column;
    }

      #teamSearch {
        border-radius: 25px;
        background-color: #f8f9fa;
        border: 2px solid #e9ecef;
        color: #212529;
        padding: 10px 20px;
        font-size: 14px;
        transition: all 0.3s ease;
        width: 250px;
      }

      #teamSearch:focus {
        background-color: #ffffff;
        border-color: #10b981;
        box-shadow: 0 0 0 0.2rem rgba(16, 185, 129, 0.25);
        color: #212529;
      }

      #teamSearch::placeholder {
        color: #6c757d;
      }
    .footer {
        margin-top: auto;
        padding:5px 11px 5px 95px;
        background-color: #dadae0;
        text-align: center;
        width: 109%;
        margin-left: -35px;
          }

      .btn-add-team {
        background: linear-gradient(135deg, #10b981 0%, #059669 100%);
        border: none;
        border-radius: 12px;
        padding: 12px 24px;
        color: white;
        font-weight: 600;
        font-size: 14px;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        transition: all 0.3s ease;
        box-shadow: 0 4px 15px rgba(16, 185, 129, 0.3);
      }
      
      .btn-add-team:hover {
        background: linear-gradient(135deg, #059669 0%, #047857 100%);
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(16, 185, 129, 0.4);
        color: white;
        text-decoration: none;
      }

      .filter-container {
        display: flex;
        align-items: center;
        gap: 10px;
        margin-bottom: 15px;
      }

      .filter-select {
        min-width: 180px;
        border: 2px solid #e9ecef;
        border-radius: 8px;
        padding: 8px 12px;
        font-size: 14px;
        background: white;
        transition: all 0.3s ease;
      }

      .filter-select:focus {
        border-color: #667eea;
        box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        outline: none;
      }

      .filter-select option[value="Billing"] {
        background-color: #3b82f6;
        color: white;
      }

      .filter-select option[value="Sales"] {
        background-color: #10b981;
        color: white;
      }

      .filter-select option[value="Support"] {
        background-color: #f59e0b;
        color: white;
      }

      .filter-select option[value="Technical"] {
        background-color: #8b5cf6;
        color: white;
      }

      .filter-select option[value="Management"] {
        background-color: #ef4444;
        color: white;
      }

      .dept-billing {
        background-color: #3b82f6;
        color: white;
        padding: 4px 8px;
        border-radius: 6px;
        font-weight: 500;
        font-size: 12px;
      }

      .dept-sales {
        background-color: #10b981;
        color: white;
        padding: 4px 8px;
        border-radius: 6px;
        font-weight: 500;
        font-size: 12px;
      }

      .dept-support {
        background-color: #f59e0b;
        color: white;
        padding: 4px 8px;
        border-radius: 6px;
        font-weight: 500;
        font-size: 12px;
      }

      .dept-technical {
        background-color: #8b5cf6;
        color: white;
        padding: 4px 8px;
        border-radius: 6px;
        font-weight: 500;
        font-size: 12px;
      }

      .dept-management {
        background-color: #ef4444;
        color: white;
        padding: 4px 8px;
        border-radius: 6px;
        font-weight: 500;
        font-size: 12px;
      }

      .dept-na {
        background-color: #6b7280;
        color: white;
        padding: 4px 8px;
        border-radius: 6px;
        font-weight: 500;
        font-size: 12px;
      }

      /* Modal Styles - Same as verify-otp.ejs */
      .form-container {
        background: linear-gradient(135deg, #0033cc 0%, #33ccff 100%);
        padding: 50px 40px 30px 40px;
        margin-top: 60px;
        border-radius: 25px;
        box-shadow: 0 30px 100px rgba(0, 0, 0, 0.2);
        width: 100%;
        position: relative;
        backdrop-filter: blur(15px);
        border: 2px solid rgba(255, 255, 255, 0.3);
        min-height: auto;
      }

      .close-btn {
        position: absolute;
        top: 20px;
        right: 20px;
        width: 40px;
        height: 40px;
        background: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%);
        border: none;
        border-radius: 50%;
        color: white;
        font-size: 20px;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.3s ease;
        box-shadow: 0 4px 15px rgba(255, 107, 107, 0.3);
      }

      .close-btn:hover {
        transform: scale(1.1) rotate(90deg);
        box-shadow: 0 6px 20px rgba(255, 107, 107, 0.4);
      }

      .form-header {
        text-align: center;
        margin-bottom: 35px;
      }

      .form-title {
        font-size: 36px;
        font-weight: 700;
        color: #ffffff;
        margin-bottom: 10px;
        text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
      }

      .form-subtitle {
        color: rgba(255, 255, 255, 0.9);
        font-size: 18px;
        line-height: 1.5;
      }

      .form-group {
        margin-bottom: 20px;
      }

      .form-label {
        font-weight: 600;
        color: #ffffff;
        margin-bottom: 12px;
        font-size: 18px;
        display: block;
        text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
      }

      .form-control {
        width: 100%;
        height: 50px;
        border: 2px solid rgba(255, 255, 255, 0.3);
        border-radius: 15px;
        padding: 12px 20px;
        font-size: 16px;
        background: linear-gradient(135deg, rgba(255, 255, 255, 0.95) 0%, rgba(255, 255, 255, 0.85) 100%);
        transition: all 0.3s ease;
        outline: none;
        backdrop-filter: blur(10px);
      }

      .form-control:focus {
        border-color: rgba(255, 255, 255, 0.8);
        box-shadow: 0 0 0 4px rgba(255, 255, 255, 0.2);
        transform: translateY(-2px);
        background: rgba(255, 255, 255, 0.98);
      }

      .form-control::placeholder {
        color: #9ca3af;
      }

      .submit-btn {
        width: 100%;
        height: 50px;
        background: linear-gradient(135deg, #00C853 0%, #00B894 100%);
        border: 2px solid rgba(255, 255, 255, 0.3);
        border-radius: 15px;
        color: white;
        font-size: 18px;
        font-weight: 700;
        letter-spacing: 1px;
        cursor: pointer;
        transition: all 0.3s ease;
        box-shadow: 0 10px 30px rgba(0, 200, 83, 0.4);
        text-transform: uppercase;
      }

      .submit-btn:hover {
        background: linear-gradient(135deg, #00E676 0%, #00C853 100%);
        transform: translateY(-3px);
        box-shadow: 0 15px 40px rgba(0, 230, 118, 0.5);
        color: white;
      }

      .submit-btn:active {
        transform: translateY(-1px);
      }

      /* Colorful Dropdown Styles */
      .form-control {
        font-weight: 600;
        border-radius: 8px;
        padding: 8px 12px;
        border: 2px solid;
        transition: all 0.3s ease;
      }

      .form-control option {
        font-weight: 600;
        padding: 10px;
      }

      /* Department Dropdown Dynamic Colors */
      .form-control.dept-Billing { background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%); border-color: #3b82f6; color: white; }
      .form-control.dept-Sales { background: linear-gradient(135deg, #10b981 0%, #059669 100%); border-color: #10b981; color: white; }
      .form-control.dept-Support { background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%); border-color: #f59e0b; color: white; }
      .form-control.dept-Technical { background: linear-gradient(135deg, #8b5cf6 0%, #6d28d9 100%); border-color: #8b5cf6; color: white; }
      .form-control.dept-Management { background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%); border-color: #ef4444; color: white; }

      /* Role Dropdown Dynamic Colors */
      .form-control.role-Team-Member { background: linear-gradient(135deg, #6b7280 0%, #4b5563 100%); border-color: #6b7280; color: white; }
      .form-control.role-Manager { background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%); border-color: #ef4444; color: white; }
      .form-control.role-Developer { background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%); border-color: #3b82f6; color: white; }
      .form-control.role-Designer { background: linear-gradient(135deg, #8b5cf6 0%, #6d28d9 100%); border-color: #8b5cf6; color: white; }
      .form-control.role-Analyst { background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%); border-color: #f59e0b; color: white; }
      .form-control.role-Support { background: linear-gradient(135deg, #10b981 0%, #059669 100%); border-color: #10b981; color: white; }

      /* Department Dropdown Option Colors */
      .form-control option[value="Billing"] { background: #3b82f6; color: white; }
      .form-control option[value="Sales"] { background: #10b981; color: white; }
      .form-control option[value="Support"] { background: #f59e0b; color: white; }
      .form-control option[value="Technical"] { background: #8b5cf6; color: white; }
      .form-control option[value="Management"] { background: #ef4444; color: white; }

      /* Role Dropdown Option Colors */
      .form-control option[value="Team Member"] { background: #6b7280; color: white; }
      .form-control option[value="Manager"] { background: #ef4444; color: white; }
      .form-control option[value="Developer"] { background: #3b82f6; color: white; }
      .form-control option[value="Designer"] { background: #8b5cf6; color: white; }
      .form-control option[value="Analyst"] { background: #f59e0b; color: white; }
      .form-control option[value="Support"] { background: #10b981; color: white; }

      /* Green Submit Button */
      .submit-btn {
        background: linear-gradient(135deg, #00C853 0%, #00B894 100%) !important;
        color: white !important;
      }

      .submit-btn:hover {
        background: linear-gradient(135deg, #00E676 0%, #00C853 100%) !important;
        color: white !important;
      }

      /* Responsive Design for Modal */
      @media (max-width: 768px) {
        .form-container {
          padding: 30px 25px;
          max-width: 100%;
        }
        
        .form-title {
          font-size: 28px;
        }
        
        .form-subtitle {
          font-size: 14px;
        }
      }

      @media (max-width: 480px) {
        .form-container {
          padding: 25px 20px;
        }
        
        .form-title {
          font-size: 24px;
        }
        
        .form-control {
          height: 50px;
          font-size: 14px;
        }
        
        .submit-btn {
          height: 50px;
          font-size: 16px;
        }
      }

      .filter-label {
        font-weight: 600;
        color: #495057;
        font-size: 14px;
        margin: 0;
      }

      @media (max-width: 768px) {
        .filter-container {
          flex-direction: column;
          align-items: flex-start;
        }
        
        .filter-select {
          width: 100%;
        }
      }
      @media screen and (max-width: 768px) {
        .hide-on-small{
          display: none;
        }
      }
      @media screen and (max-width: 1024px) {  /* wraps earlier */
  #paginatedTable {
    display: flex;
    flex-direction: column;
    width: 100%;
  }

  #paginatedTable thead {
    display: none; /* hide table header */
  }

  #paginatedTable tr {
    display: flex;
    flex-direction: column;
    border: 1px solid #e5e7eb;
    border-radius: 8px;
    margin-bottom: 10px;
    padding: 12px;
    background: #fff;
  }

  #paginatedTable td {
    border: none;
    padding: 6px 0;
  }
}


    </style>
</head>

<body>

              <%- include('../par/sidebar.ejs') %>

  <!-- Navbar Start -->
  <%- include('../par/header.ejs') %>

  <div class="body-wrapper" style=" position: relative;">
    <div class="container-fluid">
     <div class="card card-body py-3">
            <div class="row align-items-center">
                <div class="col-md-12">
                    <div class="d-flex justify-content-between align-items-center flex-wrap">
                        <div>
                            <h4 class="mb-4 mb-sm-0 card-title">All Team Members <i class="bi bi-people-fill"></i></h4>
                            <small class="text-muted">
                                Admin Access: <strong>Unlimited Members</strong> | Total: <%= users ? users.length : 0 %> members
                            </small>
                        </div>
                        <div class="d-flex align-items-center gap-2 flex-wrap">
                            <input type="text" id="teamSearch" class="form-control" style="width: 250px;" placeholder="Search team members...">
                            <a href="/AdminTeam" class="btn-add-team">
                                <i class="fas fa-user-plus"></i> Add Team Member
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Flash Messages -->
        <% if (typeof success_msg !== 'undefined' && success_msg) { %>
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <%= success_msg %>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <% } %>

        <% if (typeof error_msg !== 'undefined' && error_msg) { %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <%= error_msg %>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <% } %>

        <div class="card card-body">
            <div class="d-flex justify-content-end">
                <div class="filter-container">
                    <label for="departmentFilter" class="filter-label">Filter:</label>
                    <select class="filter-select" id="departmentFilter">
                        <option value="">All Departments</option>
                        <option value="Billing">Billing</option>
                        <option value="Sales">Sales</option>
                        <option value="Support">Support</option>
                        <option value="Technical">Technical</option>
                        <option value="Management">Management</option>
                    </select>
                </div>
            </div>
            <div class="table-responsive mt-3">
                <table class="table search-table align-middle text-nowrap" id="paginatedTable">
                    <thead class="header-item  text-white hide-on-small" style="  background: linear-gradient(135deg, #0033cc 0%, #33ccff 100%) !important; padding: 10px !important;">
                        <tr>
                            <th style="  background: linear-gradient(135deg, #0033cc 0%, #33ccff 100%) !important; padding: 10px; width: 50px;">
                                <input type="checkbox" id="selectAllTeam" class="form-check-input" style="cursor: pointer; width: 18px; height: 18px;">
                            </th>
                            <th style="  background: linear-gradient(135deg, #0033cc 0%, #33ccff 100%) !important; padding: 10px;">Name</th>
                            <th style="  background: linear-gradient(135deg, #0033cc 0%, #33ccff 100%) !important; padding: 10px;">Email</th>
                            <th style="  background: linear-gradient(135deg, #0033cc 0%, #33ccff 100%) !important; padding: 10px;">Phone</th>
                            <th style="  background: linear-gradient(135deg, #0033cc 0%, #33ccff 100%) !important; padding: 10px;">Department</th>
                            <th style="  background: linear-gradient(135deg, #0033cc 0%, #33ccff 100%) !important; padding: 10px;">Role</th>
                            <th style="  background: linear-gradient(135deg, #0033cc 0%, #33ccff 100%) !important; padding: 10px;">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (users && users.length > 0) { %>
                            <% users.forEach(user => { %>
                            <tr class="search-items" data-department="<%= user.department || '' %>">
                                <td style="color: #1f2324;">
                                    <input type="checkbox" class="team-row-checkbox form-check-input" data-id="<%= user._id %>" style="cursor: pointer; width: 18px; height: 18px;">
                                </td>
                                <td style="color: #1f2324;">
                                    <div class="d-flex align-items-center">
                                        <img src="https://img.icons8.com/3d-fluency/94/user-male--v4.png" alt="user-male--v4"/>
                                        <div class="ms-3">
                                            <div class="user-meta-info">
                                                <h6 class="user-name mb-0 text-dark"><%= user.name %></h6>
                                            </div>
                                        </div>
                                    </div>
                                </td>
                                <td style="color: #1f2324;"><span class="usr-email-addr"><%= user.email %></span></td>
                                <td style="color: #1f2324;"><span class="usr-ph-no"><%= user.phone || 'N/A' %></span></td>
                                <td style="color: #1f2324;">
                                    <span class="usr-department
                                        <% if (user.department === 'Billing') { %>dept-billing
                                        <% } else if (user.department === 'Sales') { %>dept-sales
                                        <% } else if (user.department === 'Support') { %>dept-support
                                        <% } else if (user.department === 'Technical') { %>dept-technical
                                        <% } else if (user.department === 'Management') { %>dept-management
                                        <% } else { %>dept-na<% } %>">
                                        <%= user.department || 'N/A' %>
                                    </span>
                                </td>
                                <td style="color: #1f2324;"><span class="usr-role"><%= user.role || 'N/A' %></span></td>
                                <td>
                                    <div class="action-btn">
                                        <button type="button" class="btn btn-link text-success edit"
                                                data-id="<%= user._id %>"
                                                data-name="<%= user.name %>"
                                                data-email="<%= user.email %>"
                                                data-phone="<%= user.phone || '' %>"
                                                data-department="<%= user.department || '' %>"
                                                data-role="<%= user.role || '' %>"
                                                onclick="openEditModal(this)">
                                            <i class="fa-solid fa-pen-to-square text-success"></i>
                                        </button>
                                        <button type="button" class="btn btn-link text-danger p-0"
                                                data-id="<%= user._id %>"
                                                data-name="<%= user.name %>"
                                                onclick="deleteTeamMember(this)">
                                            <i class="fa-solid fa-trash-can text-danger"></i>
                                        </button>
                                    </div>
                                </td>
                            </tr>
                            <% }); %>
                        <% } else { %>
                            <tr>
                                <td colspan="7" class="text-center py-5">
                                    <i class="bi bi-people display-1 text-muted"></i>
                                    <h4 class="mt-3 text-muted">No Team Members Yet</h4>
                                    <p class="text-muted">Start building your team by adding your first team member.</p>
                                    <a href="/AdminTeam" class="btn btn-primary">
                                        <i class="bi bi-person-plus me-2"></i>Add Your First Team Member
                                    </a>
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
            
            <!-- Delete Selected Button -->
            <div class="mb-4">
                <button id="deleteSelectedBtn"
                        data-table="adminteams"
                        class="btn btn-danger shadow-sm px-4 py-2 fw-semibold"
                        style="display: none;">
                    <i class="fas fa-trash-alt me-2"></i> Delete Selected
                </button>
            </div>
        </div>
    </div>
</div>

    <!-- footer -->
<%-include('../par/footer.ejs')%>
    <!-- footer -->

<!-- Update Team Member Modal -->
<div class="modal fade" id="updateTeamModal" tabindex="-1" aria-labelledby="updateTeamModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered modal-lg" style="max-width: 800px; min-height: auto;">
    <div class="modal-content" style="background: transparent; border: none; min-height: auto;">
      <div class="form-container">
        <button class="close-btn" data-bs-dismiss="modal" aria-label="Close">
          <i class="fas fa-times"></i>
        </button>
        
        <div class="form-header">
          <h1 class="form-title">Update Team Member</h1>
          <p class="form-subtitle">Update the team member information below</p>
        </div>
        
        <form id="updateTeamForm">
          <input type="hidden" id="updateTeamId" name="id">
          
          <div class="form-group">
            <label for="updateName" class="form-label">
              <i class="fas fa-user me-2"></i>Full Name
            </label>
            <input type="text" class="form-control" id="updateName" name="name" required placeholder="Enter full name">
          </div>
          
          <div class="form-group">
            <label for="updateEmail" class="form-label">
              <i class="fas fa-envelope me-2"></i>Email
            </label>
            <input type="email" class="form-control" id="updateEmail" name="email" required placeholder="Enter email address">
          </div>

          <div class="form-group">
            <label for="updatePhone" class="form-label">
              <i class="fas fa-phone me-2"></i>Phone
            </label>
            <input type="text" class="form-control" id="updatePhone" name="phone" placeholder="Enter phone number (optional)">
          </div>

          <div class="form-group">
            <label for="updateDepartment" class="form-label">
              <i class="fas fa-building me-2"></i>Department
            </label>
            <select class="form-control" id="updateDepartment" name="department" required onchange="this.className='form-control dept-'+this.value">
              <option value="">Select Department</option>
              <option value="Billing">Billing</option>
              <option value="Sales">Sales</option>
              <option value="Support">Support</option>
              <option value="Technical">Technical</option>
              <option value="Management">Management</option>
            </select>
          </div>

          <div class="form-group">
            <label for="updateRole" class="form-label">
              <i class="fas fa-user-tag me-2"></i>Role
            </label>
            <select class="form-control" id="updateRole" name="role" required onchange="this.className='form-control role-'+this.value.replace(/ /g, '-')">
              <option value="">Select Role</option>
              <option value="Team Member">Team Member</option>
              <option value="Manager">Manager</option>
              <option value="Developer">Developer</option>
              <option value="Designer">Designer</option>
              <option value="Analyst">Analyst</option>
              <option value="Support">Support</option>
            </select>
          </div>
  
          <button type="submit" class="submit-btn">
            <i class="fas fa-save me-2"></i>Update Team Member
          </button>
        </form>
      </div>
    </div>
  </div>
</div>



</body>
<!-- JavaScript Libraries -->
<script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"></script>

<!-- Template Javascript -->
<script src="/js/main.js"></script>
<script>
    document.addEventListener("DOMContentLoaded", () => {
        const selectAll = document.getElementById("selectAllTeam");
        const checkboxes = document.querySelectorAll(".team-row-checkbox");
        const deleteBtn = document.getElementById("deleteSelectedBtn");

        const toggleButton = () => {
            const selected = document.querySelectorAll(".team-row-checkbox:checked").length;
            deleteBtn.style.display = selected > 0 ? "inline-flex" : "none";
        };

        if (selectAll) {
            selectAll.addEventListener("change", () => {
                checkboxes.forEach(cb => {
                    if (cb.closest('tr').style.display !== 'none') {
                        cb.checked = selectAll.checked;
                    }
                });
                toggleButton();
            });
        }

        checkboxes.forEach(cb => cb.addEventListener("change", () => {
            const visibleCheckboxes = Array.from(checkboxes).filter(checkbox =>
                checkbox.closest('tr').style.display !== 'none'
            );
            const allChecked = visibleCheckboxes.length > 0 && visibleCheckboxes.every(cb => cb.checked);
            if (selectAll) selectAll.checked = allChecked;
            toggleButton();
        }));

        deleteBtn.addEventListener("click", async () => {
            const selectedIds = [...document.querySelectorAll(".team-row-checkbox:checked")].map(cb => cb.dataset.id);
            if (selectedIds.length === 0) return alert("Please select at least one team member to delete.");
            if (!confirm(`Delete ${selectedIds.length} selected team member(s)?`)) return;

            try {
                const tableName = deleteBtn.dataset.table; // "adminteams"
                const res = await fetch(`/delete-selected/${tableName}`, {
                    method: "POST",
                    headers: { "Content-Type": "application/json" },
                    body: JSON.stringify({ selectedIds }),
                });

                const data = await res.json();
                if (data.success) {
                    selectedIds.forEach(id => {
                        const row = document.querySelector(`tr[data-id="${id}"]`);
                        if (row) row.remove();
                    });
                    alert("Selected team members deleted successfully.");
                    toggleButton();
                } else {
                    alert("Error deleting team members: " + data.message);
                }
            } catch (err) {
                console.error(err);
                alert("Something went wrong.");
            }
        });

        toggleButton();
    });

    // Team search functionality
    document.addEventListener('DOMContentLoaded', function() {
        const searchInput = document.getElementById('teamSearch');
        if (searchInput) {
            searchInput.addEventListener('input', function() {
                const searchTerm = this.value.toLowerCase();
                const table = document.getElementById('paginatedTable');
                if (table) {
                    const rows = table.querySelectorAll('tbody tr');
                    rows.forEach(row => {
                        const text = row.textContent.toLowerCase();
                        row.style.display = text.includes(searchTerm) ? '' : 'none';
                    });
                }
            });
        }
    });
// Department filter functionality
document.getElementById('departmentFilter').addEventListener('change', function() {
    const selectedDepartment = this.value;
    const tableRows = document.querySelectorAll('#paginatedTable tbody tr');
    
    tableRows.forEach(row => {
        const rowDepartment = row.getAttribute('data-department');
        if (selectedDepartment === '' || rowDepartment === selectedDepartment) {
            row.style.display = '';
        } else {
            row.style.display = 'none';
        }
    });
});

// Open edit modal with pre-filled data
function openEditModal(button) {
    const id = button.getAttribute('data-id');
    const name = button.getAttribute('data-name');
    const email = button.getAttribute('data-email');
    const phone = button.getAttribute('data-phone');
    const department = button.getAttribute('data-department');
    const role = button.getAttribute('data-role');

    document.getElementById('updateTeamId').value = id;
    document.getElementById('updateName').value = name;
    document.getElementById('updateEmail').value = email;
    document.getElementById('updatePhone').value = phone;
    document.getElementById('updateDepartment').value = department;
    document.getElementById('updateRole').value = role;

    const modal = new bootstrap.Modal(document.getElementById('updateTeamModal'));
    modal.show();
}

// Handle update form submission
document.getElementById('updateTeamForm').addEventListener('submit', function(e) {
    e.preventDefault();
    
    const submitBtn = this.querySelector('.submit-btn');
    const originalText = submitBtn.innerHTML;
    submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin me-2"></i>Updating...';
    submitBtn.disabled = true;
    
    const formData = new FormData(this);
    const data = Object.fromEntries(formData);
    
    console.log('Sending update data:', data);
    
    fetch('/updateteam', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
        },
        body: JSON.stringify(data)
    })
    .then(response => {
        console.log('Response status:', response.status);
        if (!response.ok) {
            throw new Error(`HTTP error! status: ${response.status}`);
        }
        return response.json();
    })
    .then(data => {
        console.log('Response data:', data);
        if (data.success) {
            const modal = bootstrap.Modal.getInstance(document.getElementById('updateTeamModal'));
            modal.hide();
            location.reload();
        } else {
            alert(data.message || 'Error updating team member');
        }
    })
    .catch(error => {
        console.error('Error:', error);
        alert('Network error: ' + error.message);
    })
    .finally(() => {
        submitBtn.innerHTML = originalText;
        submitBtn.disabled = false;
    });
});

// Delete team member with confirmation
function deleteTeamMember(button) {
    const id = button.getAttribute('data-id');
    const name = button.getAttribute('data-name');

    if (confirm(`Are you sure you want to delete ${name}? This action cannot be undone.`)) {
        console.log('Deleting team member with ID:', id);

        fetch(`/deleteteam/${id}`, {
            method: 'POST'
        })
        .then(response => {
            console.log('Delete response status:', response.status);
            if (!response.ok) {
                throw new Error(`HTTP error! status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
            console.log('Delete response data:', data);
            if (data.success) {
                // Remove the row from table without full reload
                const row = document.querySelector(`button[onclick*="${id}"]`).closest('tr');
                if (row) {
                    row.remove();
                }
                
                // Show success message
                const alertDiv = document.createElement('div');
                alertDiv.className = 'alert alert-success alert-dismissible fade show';
                alertDiv.innerHTML = `
                    <i class="fas fa-check-circle me-2"></i>${data.message}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                `;
                document.querySelector('.card-body').prepend(alertDiv);
                
                // Auto dismiss after 3 seconds
                setTimeout(() => {
                    if (alertDiv.parentNode) {
                        alertDiv.remove();
                    }
                }, 3000);
            } else {
                alert(data.message || 'Error deleting team member');
            }
        })
        .catch(error => {
            console.error('Error:', error);
            alert('Network error: ' + error.message);
        });
    }
}
</script>
</html>