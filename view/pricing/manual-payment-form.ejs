<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manual Payment</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .payment-form-container {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            max-width: 500px;
            width: 100%;
            backdrop-filter: blur(10px);
            border: 2px solid rgba(255, 255, 255, 0.3);
        }

        .form-header {
            text-align: center;
            margin-bottom: 30px;
        }

        .form-header h2 {
            color: #333;
            margin-bottom: 10px;
            font-weight: 700;
        }

        .form-header p {
            color: #666;
            margin: 0;
        }

        .payment-icon {
            font-size: 48px;
            margin-bottom: 15px;
        }

        .payment-method-name {
            font-size: 24px;
            font-weight: bold;
            color: #667eea;
            margin-bottom: 20px;
        }

        .form-control {
            border-radius: 10px;
            padding: 12px 16px;
            border: 2px solid #e0e0e0;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        .form-label {
            font-weight: 600;
            color: #333;
            margin-bottom: 8px;
        }

        .amount-display {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 15px;
            border-radius: 10px;
            text-align: center;
            margin-bottom: 20px;
        }

        .amount-display .amount {
            font-size: 24px;
            font-weight: bold;
        }

        .amount-display .plan {
            font-size: 16px;
            opacity: 0.9;
        }

        .btn-submit {
            background: linear-gradient(135deg, #00C853 0%, #00B894 100%);
            border: none;
            border-radius: 12px;
            color: white;
            padding: 15px 30px;
            font-size: 18px;
            font-weight: 700;
            width: 100%;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .btn-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 184, 148, 0.4);
        }

        .back-link {
            text-align: center;
            margin-top: 20px;
        }

        .back-link a {
            color: #667eea;
            text-decoration: none;
            font-weight: 600;
        }

        .back-link a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="payment-form-container">
                    <div class="form-header">
                        <div class="payment-icon">
                            <% if (paymentMethod === 'jazzcash') { %>
                                <i class="bi bi-credit-card text-success"></i>
                            <% } else if (paymentMethod === 'payfast') { %>
                                <i class="bi bi-credit-card text-warning"></i>
                            <% } else { %>
                                <i class="bi bi-cash-coin text-info"></i>
                            <% } %>
                        </div>
                        <div class="payment-method-name">
                            <%= paymentMethod.toUpperCase() %> Payment
                        </div>
                        <h2>Enter Transaction Details</h2>
                        <p>Please provide your payment transaction details below</p>
                    </div>

                    <% if (error_msg && error_msg.length > 0) { %>
                        <div class="alert alert-danger" role="alert">
                            <%= error_msg %>
                        </div>
                    <% } %>

                    <% if (success_msg && success_msg.length > 0) { %>
                        <div class="alert alert-success" role="alert">
                            <%= success_msg %>
                        </div>
                    <% } %>

                    <div class="amount-display">
                        <div class="plan"><%= plan %> Plan</div>
                        <div class="amount">£<%= amount %></div>
                    </div>

                    <form action="/manual-payment" method="POST">
                        <div class="mb-3">
                            <label for="transfer_id" class="form-label">
                                <%= paymentMethod.toUpperCase() %> Transaction ID *
                            </label>
                            <input type="text"
                                   id="transfer_id"
                                   name="transfer_id"
                                   class="form-control"
                                   placeholder="Enter your <%= paymentMethod.toUpperCase() %> transaction ID"
                                   required>
                            <div class="form-text">
                                Please enter the exact transaction ID from your <%= paymentMethod.toUpperCase() %> payment
                            </div>
                        </div>

                        <div class="mb-3">
                            <label for="amount" class="form-label">Amount *</label>
                            <input type="number"
                                   id="amount"
                                   name="amount"
                                   class="form-control"
                                   placeholder="Amount"
                                   value="<%= amount %>"
                                   step="0.01"
                                   required>
                            <div class="form-text">
                                Amount should match: £<%= amount %> (auto-filled)
                            </div>
                        </div>

                        <button type="submit" class="btn btn-submit">
                            Submit Payment Details
                        </button>
                    </form>

                    <div class="back-link">
                        <a href="/pricing">&larr; Back to Pricing</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>