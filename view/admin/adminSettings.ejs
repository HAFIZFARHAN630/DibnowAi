<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Settings</title>

    <link rel="icon" href="\images\Dib Now 16.svg" type="image/x-icon">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css" rel="stylesheet">
    <link href="\bootstrap.css" rel="stylesheet">
    <link href="\styles.css" rel="stylesheet">
    <link href="\styles1.css" rel="stylesheet">

    <style>
        .settings-container {
            background: #F3F3F9;
            min-height: 100vh;
            padding: 40px 20px;
        }
        
        .settings-card {
            background: #F3F3F9;
            border-radius: 25px;
            box-shadow: 0 30px 100px rgba(0, 0, 0, 0.1);
            max-width: 1200px;
            margin: 0 auto;
            padding: 50px;
            border: 2px solid rgba(0, 0, 0, 0.1);
        }
        
        .settings-header {
            text-align: center;
            margin-bottom: 40px;
        }
        
        .settings-header h2 {
            font-size: 36px;
            font-weight: 700;
            color: #333333;
            margin-bottom: 10px;
        }
        
        .settings-header p {
            color: #666666;
            font-size: 16px;
        }
        
        .form-section {
            background: #ffffff;
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 25px;
            border: 1px solid rgba(0, 0, 0, 0.1);
        }
        
        .form-label {
            font-weight: 600;
            color: #333333;
            margin-bottom: 12px;
            font-size: 16px;
            display: block;
        }
        
        .form-control, .form-textarea {
            width: 100%;
            border: 2px solid #e0e0e0;
            border-radius: 12px;
            padding: 12px 20px;
            font-size: 16px;
            background: #ffffff;
            transition: all 0.3s ease;
            color: #333;
        }
        
        .form-control:focus, .form-textarea:focus {
            border-color: #0033cc;
            box-shadow: 0 0 0 4px rgba(0, 51, 204, 0.1);
            background: #ffffff;
            outline: none;
        }
        
        .favicon-preview {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            object-fit: cover;
            border: 3px solid #e0e0e0;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .favicon-preview:hover {
            transform: scale(1.05);
            border-color: #0033cc;
        }
        
        .color-picker-wrapper {
            display: flex;
            align-items: center;
            gap: 15px;
            background: #f8f9fa;
            padding: 15px;
            border-radius: 10px;
        }
        
        .color-picker {
            width: 60px;
            height: 60px;
            border: 3px solid #e0e0e0;
            border-radius: 10px;
            cursor: pointer;
        }
        
        .submit-btn {
            width: 100%;
            max-width: 400px;
            height: 60px;
            background: linear-gradient(135deg, #0033cc 0%, #0055ff 100%);
            border: none;
            border-radius: 15px;
            color: #ffffff;
            font-size: 18px;
            font-weight: 700;
            letter-spacing: 1px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 10px 30px rgba(0, 51, 204, 0.3);
            text-transform: uppercase;
            margin: 30px auto 0;
            display: block;
        }
        
        .submit-btn:hover {
            background: linear-gradient(135deg, #0055ff 0%, #0033cc 100%);
            transform: translateY(-3px);
            box-shadow: 0 15px 40px rgba(0, 51, 204, 0.4);
        }
        
        .icon-label {
            display: flex;
            align-items: center;
            gap: 10px;
            color: #666666;
            font-size: 14px;
            margin-top: 10px;
        }
        
        @media (max-width: 768px) {
            .settings-card {
                padding: 30px 20px;
            }
            
            .settings-header h2 {
                font-size: 28px;
            }
        }
    </style>
</head>
<body>
    <%- include('../par/sidebar.ejs') %>
    <%- include('../par/header.ejs') %>

    <div class="settings-container">
        <div class="settings-card">
            <div class="settings-header">
                <h2><i class="bi bi-gear-fill me-3"></i>Admin Settings</h2>
                <p>Configure your website settings and appearance</p>
            </div>

            <form action="/save-settings" method="POST" enctype="multipart/form-data">
                <!-- Site Icon & Title Section -->
                <div class="form-section">
                    <div class="row align-items-center">
                        <div class="col-md-3 text-center mb-3 mb-md-0">
                            <input type="file" id="faviconInput" name="favicon" class="d-none" aria-label="favicon">
                            <label for="faviconInput" class="d-block">
                                <img id="faviconPreview"
                                     src="<%= info?.favicon ? '/uploads/' + info.favicon : '/images/Dib Now 16.svg' %>"
                                     alt="Site Icon"
                                     class="favicon-preview">
                                <div class="icon-label">
                                    <i class="bi bi-upload"></i>
                                    <span>Click to change icon</span>
                                </div>
                            </label>
                        </div>
                        <div class="col-md-9">
                            <label for="title" class="form-label">Website Title</label>
                            <input id="title" name="title" type="text"
                                   value="<%= info.title %>"
                                   class="form-control"
                                   placeholder="Enter website title">
                        </div>
                    </div>
                </div>

                <!-- Footer Description Section -->
                <div class="form-section">
                    <label for="description" class="form-label">Footer Description</label>
                    <textarea id="description" name="description" rows="3"
                              class="form-control form-textarea"
                              placeholder="Enter footer description"><%= info.description %></textarea>
                </div>

                <!-- Welcome Description & Navbar Color Section -->
                <div class="row">
                    <div class="col-md-7 mb-4 mb-md-0">
                        <div class="form-section h-100">
                            <label for="welcome-description" class="form-label">Welcome Description</label>
                            <textarea id="welcome-description" name="welcome_description" rows="6"
                                      class="form-control form-textarea"
                                      placeholder="Enter welcome message"><%= info.welcome_description %></textarea>
                        </div>
                    </div>
                    <div class="col-md-5">
                        <div class="form-section h-100">
                            <label for="navbar-color" class="form-label">Navbar Color</label>
                            <div class="color-picker-wrapper">
                                <input id="navbar-color" name="navbar_color" type="color"
                                       value="<%= info.navbar_color %>"
                                       class="color-picker" aria-label="navbar color picker">
                                <div style="color: #333333;">
                                    <div class="fw-bold mb-1">Choose Color</div>
                                    <small>Pick the navigation bar color</small>
                                </div>
                            </div>
                            <p class="mt-3 mb-0" style="color: #666666; font-size: 13px;">
                                <i class="bi bi-info-circle me-1"></i>
                                Tip: Choose a color with good contrast for readability
                            </p>
                        </div>
                    </div>
                </div>

                <!-- Submit Button -->
                <button type="submit" class="submit-btn">
                    <i class="bi bi-save me-2"></i>Save Settings
                </button>
            </form>
        </div>
    </div>






    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="js/main.js"></script>
    
    <script>
        // Preview favicon on file select
        document.getElementById('faviconInput').addEventListener('change', function(e) {
            const file = e.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    document.getElementById('faviconPreview').src = e.target.result;
                };
                reader.readAsDataURL(file);
            }
        });
    </script>

  <!-- footer -->
  <%- include('../par/footer.ejs') %>
  <!-- footer -->
</body>
</html>