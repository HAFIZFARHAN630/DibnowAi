<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>All User</title>
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <meta content="" name="keywords">
    <meta content="" name="description">

   <!-- Favicon -->
   <link rel="icon" href="\images\Dib Now 16.svg" type="image/x-icon">

    <!-- Icon Font Stylesheet -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Customized Bootstrap Stylesheet -->
    <link href="bootstrap.css" rel="stylesheet">
    <!-- Template Stylesheet -->
    <link href="styles.css" rel="stylesheet">
    <link href="styles1.css" rel="stylesheet">

    <style>
      .btn-gradient {
        background: linear-gradient(135deg, #10b981 0%, #059669 100%);
        border: 2px solid rgba(16, 185, 129, 0.3);
        border-radius: 15px;
        color: white;
        font-weight: 700;
        padding: 12px 25px;
        transition: all 0.3s ease;
        box-shadow: 0 10px 30px rgba(16, 185, 129, 0.2);
        text-transform: uppercase;
        letter-spacing: 1px;
      }
      
      .btn-gradient:hover {
        background: linear-gradient(135deg, #059669 0%, #047857 100%);
        transform: translateY(-3px);
        box-shadow: 0 15px 40px rgba(16, 185, 129, 0.3);
        color: white;
      }

      .modal-dialog {
        max-width: 800px;
        width: 100%;
      }

      .modal-content.gradient-modal {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
        border-radius: 25px;
        box-shadow: 0 30px 100px rgba(0, 0, 0, 0.2);
        backdrop-filter: blur(15px);
        border: 2px solid rgba(255, 255, 255, 0.3);
        position: relative;
        margin-top: 80px;
      }

      .modal-header.gradient-header {
        border: none;
        text-align: center;
        padding: 35px 50px 20px;
      }

      .modal-title.gradient-title {
        font-size: 36px;
        font-weight: 700;
        color: #ffffff;
        text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
        width: 100%;
      }

      .close-btn {
        position: absolute;
        top: 20px;
        right: 20px;
        width: 40px;
        height: 40px;
        background: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%);
        border: none;
        border-radius: 50%;
        color: white;
        font-size: 20px;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.3s ease;
        box-shadow: 0 4px 15px rgba(255, 107, 107, 0.3);
      }

      .close-btn:hover {
        transform: scale(1.1) rotate(90deg);
        box-shadow: 0 6px 20px rgba(255, 107, 107, 0.4);
      }

      .modal-body.gradient-body {
        padding: 0 50px 50px;
      }

      .form-label.gradient-label {
        font-weight: 600;
        color: #ffffff;
        margin-bottom: 12px;
        font-size: 18px;
        text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
      }

      .form-control.gradient-input {
        height: 60px;
        border: 2px solid rgba(255, 255, 255, 0.3);
        border-radius: 15px;
        padding: 18px 25px;
        font-size: 18px;
        background: linear-gradient(135deg, rgba(255, 255, 255, 0.95) 0%, rgba(255, 255, 255, 0.85) 100%);
        transition: all 0.3s ease;
        backdrop-filter: blur(10px);
      }

      .form-control.gradient-input:focus {
        border-color: rgba(255, 255, 255, 0.8);
        box-shadow: 0 0 0 4px rgba(255, 255, 255, 0.2);
        transform: translateY(-2px);
        background: rgba(255, 255, 255, 0.98);
        outline: none;
      }

      .form-select.gradient-input {
        height: 60px;
        border: 2px solid rgba(255, 255, 255, 0.3);
        border-radius: 15px;
        padding: 18px 25px;
        font-size: 18px;
        background: linear-gradient(135deg, rgba(255, 255, 255, 0.95) 0%, rgba(255, 255, 255, 0.85) 100%);
        transition: all 0.3s ease;
        backdrop-filter: blur(10px);
      }

      .form-select.gradient-input:focus {
        border-color: rgba(255, 255, 255, 0.8);
        box-shadow: 0 0 0 4px rgba(255, 255, 255, 0.2);
        transform: translateY(-2px);
        background: rgba(255, 255, 255, 0.98);
        outline: none;
      }

      .submit-btn {
        width: 100%;
        height: 60px;
        background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%);
        border: 2px solid rgba(255, 255, 255, 0.3);
        border-radius: 15px;
        color: #667eea;
        font-size: 20px;
        font-weight: 700;
        letter-spacing: 1px;
        cursor: pointer;
        transition: all 0.3s ease;
        box-shadow: 0 10px 30px rgba(255, 255, 255, 0.2);
        text-transform: uppercase;
      }

      .submit-btn:hover {
        background: linear-gradient(135deg, #f8fafc 0%, #ffffff 100%);
        transform: translateY(-3px);
        box-shadow: 0 15px 40px rgba(255, 255, 255, 0.3);
        color: #764ba2;
      }

      @media (max-width: 768px) {
        .modal-content.gradient-modal {
          margin: 80px 15px 15px;
        }
        
        .modal-header.gradient-header {
          padding: 30px 25px 20px;
        }
        
        .modal-title.gradient-title {
          font-size: 28px;
        }
        
        .modal-body.gradient-body {
          padding: 0 25px 30px;
        }
      }

      @media (max-width: 480px) {
        .modal-header.gradient-header {
          padding: 25px 20px 20px;
        }
        
        .modal-title.gradient-title {
          font-size: 24px;
        }
        
        .modal-body.gradient-body {
          padding: 0 20px 25px;
        }
        
        .form-control.gradient-input,
        .form-select.gradient-input {
          height: 50px;
          font-size: 14px;
        }
        
        .submit-btn {
          height: 50px;
          font-size: 16px;
        }
      }
    </style>

</head>
<body>  
      <%- include('../par/sidebar.ejs') %>


 <!-- Navbar Start -->
<%- include('../par/header.ejs') %>
<!-- Navbar End -->

<!-- content -->
<div class="body-wrapper">
    <div class="container-fluid">
      <div class="card card-body py-3">
        <div class="row align-items-center">
          <div class="col-md-12 ">
            <div class="d-sm-flex align-items-center justify-content-between">
              <h4 class="mb-4 mb-sm-0 card-title me-1">All User <i class="bi bi-people-fill"></i></h4>
              <button type="button" class="btn btn-gradient" data-bs-toggle="modal" data-bs-target="#addUserModal">
                + Add User
              </button>
            </div>
          </div>
        </div>
      </div>
<!-- content -->

      <!-- Clients -->
      <div class="card card-body">
        <div class="table-responsive">
          <table class="table search-table align-middle text-nowrap" id="paginatedTable">
            <thead class="header-item text-center">
              <tr>
                <th>First Name</th>
                <th>Email</th>
                <th>Phone Number</th>
                <th>Company</th>
                <th>Plane</th>
                <th class="text-center">Action</th>
              </tr>
            </thead>
            <tbody class="text-center">
              <% users.forEach((user) => { %>
                <% if (user.role !== "admin") { %> <!-- Condition to exclude admins -->
                  <tr>
                    <td><%= user.first_name %></td>
                    <td><%= user.email %></td>
                    <td><%= user.phone_number %></td>
                    <td><%= user.company %></td>
                    <td><%= user.plan_name %></td>
                    <td>

                      <!-- update  -->
                      <div class="d-flex justify-content-center align-items-center">

                        <button type="button" class="btn btn-link text-primary edit" data-bs-toggle="modal" data-bs-target="#editUserModal<%= user.id %>"> 
                          <i class="fs-4 fa-solid fa-pen-to-square text-primary"></i>
                        </button>
                        <!-- update  -->

                        <form action="/admin/delete/<%= user.id %>" method="POST">
                          <button type="submit" class="button close-button" style="border: none; background-color: transparent;">
                            <i class="fa-solid fa-trash-can text-danger"></i>
                          </button>
                        </form>
                      </div>
                    </td>
                  </tr>
                <% } %>
              <% }); %>
            </tbody>
            
          </table>
        </div>
      </div>  
    <!-- Content End -->

<!-- Add User Modal -->
<div class="modal fade" id="addUserModal" tabindex="-1" aria-labelledby="addUserModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content gradient-modal">
      <button class="close-btn" data-bs-dismiss="modal" aria-label="Close">
        <i class="fas fa-times"></i>
      </button>
      
      <div class="modal-header gradient-header">
        <h1 class="modal-title gradient-title" id="addUserModalLabel">Add User</h1>
      </div>
      
      <div class="modal-body gradient-body">
        <form action="/admin/addUser" method="POST">
          <div class="row">
            <div class="mb-3 col-md-6">
              <label for="add_first_name" class="form-label gradient-label">
                <i class="fas fa-user me-2"></i>First Name
              </label>
              <input type="text" class="form-control gradient-input" id="add_first_name" name="first_name" required placeholder="Enter first name">
            </div>
            
            <div class="mb-3 col-md-6">
              <label for="add_email" class="form-label gradient-label">
                <i class="fas fa-envelope me-2"></i>Email
              </label>
              <input type="email" class="form-control gradient-input" id="add_email" name="email" required placeholder="Enter email address">
            </div>

            <div class="mb-3 col-md-6">
              <label for="add_phone_number" class="form-label gradient-label">
                <i class="fas fa-phone me-2"></i>Phone Number
              </label>
              <input type="text" class="form-control gradient-input" id="add_phone_number" name="phone_number" required placeholder="Enter phone number">
            </div>

            <div class="mb-3 col-md-6">
              <label for="add_company" class="form-label gradient-label">
                <i class="fas fa-building me-2"></i>Company
              </label>
              <input type="text" class="form-control gradient-input" id="add_company" name="company" required placeholder="Enter company name">
            </div>

            <div class="mb-3 col-md-6">
              <label for="add_plan_name" class="form-label gradient-label">
                <i class="fas fa-crown me-2"></i>Plan
              </label>
              <select class="form-select gradient-input" id="add_plan_name" name="plan_name" required>
                <option value="">Select Plan</option>
                <option value="FREE TRIAL">FREE TRIAL</option>
                <option value="BASIC">BASIC</option>
                <option value="STANDARD">STANDARD</option>
                <option value="PREMIUM">PREMIUM</option>
              </select>
            </div>

            <div class="mb-3 col-md-6">
              <label for="add_plan_limit" class="form-label gradient-label">
                <i class="fas fa-hashtag me-2"></i>Add Limit
              </label>
              <input type="number" class="form-control gradient-input" id="add_plan_limit" name="plan_limit" required placeholder="Enter limit">
            </div>
          </div>
  
          <button type="submit" class="submit-btn">
            <i class="fas fa-user-plus me-2"></i>Add User
          </button>
        </form>
      </div>
    </div>
  </div>
</div>

<!-- Update User Modals -->
<% users.forEach((user) => { %>
  <% if (user.role !== "admin") { %> <!-- Exclude admins -->
    <div class="modal fade" id="editUserModal<%= user.id %>" tabindex="-1" aria-labelledby="editUserModalLabel<%= user.id %>" aria-hidden="true">
      <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content gradient-modal">
          <button class="close-btn" data-bs-dismiss="modal" aria-label="Close">
            <i class="fas fa-times"></i>
          </button>
          
          <div class="modal-header gradient-header">
            <h1 class="modal-title gradient-title" id="editUserModalLabel<%= user.id %>">Update User</h1>
          </div>
          
          <div class="modal-body gradient-body">
            <form action="/admin/update/<%= user.id %>" method="POST">
              <div class="row">
                <div class="mb-3 col-md-6">
                  <label for="first_name_<%= user.id %>" class="form-label gradient-label">
                    <i class="fas fa-user me-2"></i>First Name
                  </label>
                  <input type="text" class="form-control gradient-input" id="first_name_<%= user.id %>" name="first_name" value="<%= user.first_name %>" required>
                </div>
                
                <div class="mb-3 col-md-6">
                  <label for="email_<%= user.id %>" class="form-label gradient-label">
                    <i class="fas fa-envelope me-2"></i>Email
                  </label>
                  <input type="email" class="form-control gradient-input" id="email_<%= user.id %>" name="email" value="<%= user.email %>" required>
                </div>

                <div class="mb-3 col-md-6">
                  <label for="phone_number_<%= user.id %>" class="form-label gradient-label">
                    <i class="fas fa-phone me-2"></i>Phone Number
                  </label>
                  <input type="text" class="form-control gradient-input" id="phone_number_<%= user.id %>" name="phone_number" value="<%= user.phone_number %>" required>
                </div>

                <div class="mb-3 col-md-6">
                  <label for="company_<%= user.id %>" class="form-label gradient-label">
                    <i class="fas fa-building me-2"></i>Company
                  </label>
                  <input type="text" class="form-control gradient-input" id="company_<%= user.id %>" name="company" value="<%= user.company %>" required>
                </div>

                <div class="mb-3 col-md-6">
                  <label for="plan_name_<%= user.id %>" class="form-label gradient-label">
                    <i class="fas fa-crown me-2"></i>Plan
                  </label>
                  <select class="form-select gradient-input" id="plan_name_<%= user.id %>" name="plan_name" required>
                    <option value="FREE TRIAL" <%= user.plan_name === 'FREE TRIAL' ? 'selected' : '' %>>FREE TRIAL</option>
                    <option value="BASIC" <%= user.plan_name === 'BASIC' ? 'selected' : '' %>>BASIC</option>
                    <option value="STANDARD" <%= user.plan_name === 'STANDARD' ? 'selected' : '' %>>STANDARD</option>
                    <option value="PREMIUM" <%= user.plan_name === 'PREMIUM' ? 'selected' : '' %>>PREMIUM</option>
                  </select>
                </div>
    
                <div class="mb-3 col-md-6">
                  <label for="plan_limit_<%= user.id %>" class="form-label gradient-label">
                    <i class="fas fa-hashtag me-2"></i>Add Limit
                  </label>
                  <input type="number" class="form-control gradient-input" id="plan_limit_<%= user.id %>" name="plan_limit" value="<%= user.plan_limit %>" required>
                </div>
              </div>
      
              <button type="submit" class="submit-btn">
                <i class="fas fa-save me-2"></i>Update User
              </button>
            </form>
          </div>
        </div>
      </div>
    </div>
  <% } %>
<% }) %>


</body>
  <!-- JavaScript Libraries -->
  <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"></script>
  <!-- Socket.IO Client -->
  <script src="/socket.io/socket.io.js"></script>

  <!-- Template Javascript -->
  <script src="js/main.js"></script>

</html>


