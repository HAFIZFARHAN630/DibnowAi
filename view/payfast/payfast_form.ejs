<!DOCTYPE html>
<html lang="en">
<head>
    <title>PayFast Payment Gateway</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Favicon -->
    <link rel="icon" href="/images/Dib Now 16.svg" type="image/x-icon">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            margin: 0;
            padding: 0;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .payment-container {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
            padding: 40px;
            max-width: 500px;
            width: 100%;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .payment-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #ff6b6b, #4ecdc4, #45b7d1, #96ceb4);
        }

        .payment-header {
            margin-bottom: 30px;
        }

        .payment-header h2 {
            color: #2c3e50;
            font-weight: 700;
            margin-bottom: 10px;
            font-size: 28px;
        }

        .payment-header .subtitle {
            color: #7f8c8d;
            font-size: 16px;
            margin-bottom: 0;
        }

        .payment-details {
            background: #f8f9fa;
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 30px;
            border-left: 5px solid #4ecdc4;
        }

        .detail-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
            padding: 10px 0;
            border-bottom: 1px solid #e9ecef;
        }

        .detail-row:last-child {
            border-bottom: none;
            margin-bottom: 0;
        }

        .detail-label {
            font-weight: 600;
            color: #495057;
            font-size: 14px;
        }

        .detail-value {
            color: #2c3e50;
            font-weight: 700;
            font-size: 16px;
        }

        .amount-highlight {
            background: linear-gradient(135deg, #4ecdc4, #44a08d);
            color: white;
            padding: 15px;
            border-radius: 10px;
            margin: 20px 0;
        }

        .amount-highlight .detail-value {
            color: white;
            font-size: 24px;
            font-weight: 800;
        }

        .loading-spinner {
            display: none;
            margin: 20px 0;
        }

        .spinner {
            width: 40px;
            height: 40px;
            border: 4px solid #f3f3f3;
            border-top: 4px solid #4ecdc4;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin: 0 auto;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .status-message {
            margin-top: 20px;
            padding: 15px;
            border-radius: 10px;
            font-weight: 600;
        }

        .status-message.success {
            background-color: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }

        .status-message.error {
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }

        .payfast-logo {
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, #ff6b6b, #4ecdc4);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            font-size: 24px;
            font-weight: bold;
            color: white;
        }

        .secure-badge {
            position: absolute;
            top: 20px;
            right: 20px;
            background: #28a745;
            color: white;
            padding: 5px 10px;
            border-radius: 15px;
            font-size: 12px;
            font-weight: 600;
        }
    </style>
</head>
<body>
    <div class="payment-container">
        <div class="secure-badge">🔒 Secure</div>

        <div class="payment-header">
            <div class="payfast-logo">PF</div>
            <h2>PayFast Payment Gateway</h2>
            <p class="subtitle">Complete your secure payment</p>
        </div>

        <div class="payment-details">
            <div class="detail-row">
                <span class="detail-label">Plan:</span>
                <span class="detail-value"><%= plan %></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Customer:</span>
                <span class="detail-value"><%= user.first_name %> <%= user.last_name %></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Email:</span>
                <span class="detail-value"><%= user.email %></span>
            </div>
        </div>

        <div class="amount-highlight">
            <div class="detail-row">
                <span class="detail-label">Plan Price:</span>
                <span class="detail-value">£<%= amount %> GBP</span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Amount in PKR:</span>
                <span class="detail-value">PKR <%= (parseFloat(amount) * 397.1863).toFixed(2) %></span>
            </div>
        </div>

        <div class="loading-spinner" id="loadingSpinner">
            <div class="spinner"></div>
            <p style="margin-top: 10px; color: #6c757d;">Redirecting to PayFast...</p>
        </div>

        <div id="statusMessage"></div>

        <!-- Hidden form that will auto-submit to PayFast -->
        <form id="payfastForm" action="<%= process.env.PAYFAST_HPP_URL %>" method="post" style="display: none;">
            <input type="hidden" name="MERCHANT_ID" value="<%= merchantId %>">
            <input type="hidden" name="TOKEN" value="<%= token %>">
            <input type="hidden" name="BASKET_ID" value="<%= basketId %>">
            <input type="hidden" name="TXNAMT" value="<%= txnAmount %>">
            <input type="hidden" name="CURRENCY_CODE" value="<%= currencyCode %>">
            <input type="hidden" name="SUCCESS_URL" value="<%= successUrl %>">
            <input type="hidden" name="FAILURE_URL" value="<%= failureUrl %>">
            <input type="hidden" name="ORDER_DATE" value="<%= orderDate %>">

            <!-- Debug information (remove in production) -->
            <input type="hidden" name="DEBUG" value="false">
        </form>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            console.log('🚀 PayFast form loaded, preparing to submit...');

            // Show loading spinner
            document.getElementById('loadingSpinner').style.display = 'block';

            // Submit form after a short delay to show loading state
            setTimeout(() => {
                const form = document.getElementById('payfastForm');

                console.log('📝 Submitting PayFast form with data:', {
                    merchantId: '<%= merchantId %>',
                    basketId: '<%= basketId %>',
                    amount: '<%= txnAmount %>',
                    currency: '<%= currencyCode %>',
                    action: form.action,
                    token: '<%= token %>'.substring(0, 20) + '...'
                });

                // Submit the form
                form.submit();

                console.log('✅ PayFast form submitted successfully');
            }, 1500); // 1.5 second delay to show loading state
        });

        // Add error handling for form submission
        window.addEventListener('error', function(e) {
            console.error('❌ Error during PayFast form submission:', e.error);

            document.getElementById('loadingSpinner').style.display = 'none';

            const statusDiv = document.getElementById('statusMessage');
            statusDiv.innerHTML = `
                <div class="status-message error">
                    <i class="fas fa-exclamation-triangle"></i>
                    Error submitting payment form. Please try again.
                </div>
            `;
        });
    </script>

    <!-- Font Awesome for icons -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/js/all.min.js"></script>
</body>
</html>